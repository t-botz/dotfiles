# Dotfiles

Tibo's dotfiles.

```sh
./bootstrap.sh  # install Determinate Nix, build, and activate (requires sudo)
./rebuild.sh    # apply subsequent edits
```

To update Nix dependencies, run `nix flake update` and then `./rebuild.sh`.
